<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maxin.liang">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:name=".common.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/cangku"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.GoodsListActivity" />
        <activity android:name=".activity.GoodsInfoActivity" />
        <activity android:name=".activity.WebViewActivity" />
        <activity android:name=".activity.MasterItemActivity" />
        <activity android:name=".activity.BrandInfoActivity" />
        <activity android:name=".activity.MGZActivity" />
        <activity android:name=".activity.tuijian.TuiJianVideoInfoActivity" />
        <activity android:name=".activity.tuijian.TuiJianTextInfoActivity" />
        <activity android:name=".activity.tuijian.TuiJianImageInfoActivity" />
        <activity android:name=".activity.tuijian.TuiJianGifInfoActivity"></activity>
    </application>

</manifest>